# ZIME 项目交付规则

## 本机修复交付

用户要求：每次修复验证通过后，默认构建并安装到本机，不再只交付源码或再次询问是否安装。
若本次用户明确要求只分析、只改代码或不要安装，以本次要求为准。
此授权仅适用于本项目在用户当前 Mac 上的修复任务，不适用于 CI、其他机器或纯审查任务。

- 先完成与改动风险相称的测试，递增版本/构建号，构建并验证正式身份的安装候选。
- 通过已有的 `zime-install-helper update` 事务安装，保留当前用户的设置、词库、学习数据和回滚备份。
- 安装后验证实际版本、签名、插件、运行状态及数据保留；失败时保留恢复材料，不继续清理旧包。
- 不自动推送或发布 GitHub；发布仍需用户另行要求。

## 安装包保留

成功安装并核验后，仅保留包括最新版在内的最近 **3 个版本** 的本地 ZIME 安装/更新包。
按实际包版本而非文件修改时间计算代数；同一版本的 ZIP、PKG 和解压安装目录属于同一代。
只改动本项目明确识别出的安装包和安装暂存目录；先列出保留/淘汰清单，再清理，最后复查。
优先移入废纸篓，恢复区内的文件不计入可用安装包代数，不清空废纸篓。

不得把已安装的 `~/Library/Input Methods/ZIME.app`、
`~/Library/Application Support/ZIME`、安装事务回滚备份、源码、文档或测试日志当作旧安装包删除。
不足 3 代时不删除现有包，不从废纸篓恢复更老的版本来凑数。
