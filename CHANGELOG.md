# ZIME 版本记录

本文件只记录用户安装后能够感知的程序功能、问题修复、交互优化和安装更新体验。
README、文档、CI、构建及发布脚本调整不属于版本更新。继承的 Squirrel 历史请查阅
[Squirrel changelog](https://github.com/rime/squirrel/blob/master/CHANGELOG.md)。

## ZIME 0.1.20 — 2026-09-11

本次公开发布汇总 0.1.18–0.1.20 的本地修复；上一公开版本为 0.1.17。

### 拼音纠错与候选排序

- 修复全拼 `nui` 被冷门英文 `niu / nui` 抢占的问题，支持 `nui → 牛`、
  `nuinai → 牛奶` 等末尾字母换位纠错；保留 `dui / gui / hui / shui / tui`
  等合法拼音，不改变双拼键位。
- 修复中文词库内的英文缩写被当作中文优先项：冷启动输入 `i` 时，精确候选 `I`
  不再被 `IME / IBM` 等补全抢占；同一输入范围内的英文精确拼写优先于普通别名和补全。
- 英文缩写、别名和补全按实际输入学习。输入 `ime` 选择 `IME`，不会自动把它排到
  `i` 的最前面；在 `i` 下主动选择则仍会提升。中文模式内中英文、Emoji 共同排序，
  独立英文模式继续单独学习；支持双向调整、跨重启保存和关闭学习。

### 混合输入

- 拒绝将未输完整的英文缩写拼接成整句：`woi / woim / woib` 不再误扩成
  “我IME / 我IBM”，完整输入 `woime / woibm` 仍可正常组句。
- 有中文前缀、但没有可靠整段匹配时，提供保留未匹配字母的完整候选，
  如 `wov → 我v`、`woi → 我i`、`nihaov → 你好v`。数字键一次提交整段，
  Enter 仍提交原始输入，不会把选中“我v”学成 `wo` 的词条。
- 保留原生分段选词入口；整段备选的学习不随候选行数 3–9 的调整而丢失。
  正常全拼、简拼、完整中英混输、显式大写、数字和代码输入沿用既有规则。

### 安装与兼容

- 同时提供完整 ZIP、PKG 和 Core-only ZIP；已有 0.1.17 完整数据可只更新 Core。
  本次不更换上游词库或 LTS 模型，新拼音规则通过 Core 部署生效，无需清空学习记录。
- 继续保留设置、个人词频和安装回滚材料。支持 Apple Silicon、macOS 13+；
  仍为 Ad-hoc 开发预览，未经过 Apple 公证，未开启自动更新频道。

完整下载说明与验证范围见 [0.1.20 版本说明](docs/releases/ZIME-0.1.20.md)。

## ZIME 0.1.17 — 2026-09-08

- 更新锁定的万象词库、LTS 模型、librime-octagram 和 librime-lua。
- 恢复完整 ZIP、PKG 和 Core-only ZIP 发布，统一保留个人数据的安装事务。
- 详细变更与限制见 [0.1.17 版本说明](docs/releases/ZIME-0.1.17.md)。

---

以下为继承的 **上游 Linnet 历史记录**，版本号与 ZIME 独立，不代表 ZIME 当前功能；
例如下述“可展开”候选网格已不在 ZIME 中提供。更早的 ZIME 变更见 README 的版本索引。

## 0.1.15 — 2026-09-04

### 候选网格

- “可展开”候选改为 macOS 式多行网格：左右键逐项移动，上下键在相邻行的同一列移动；高亮在可见网格内移动时窗口不再反复滚动或只显示第一行，越过边界后才整体推进一行。
- 横排和竖排偏好在紧凑状态下保持各自布局，展开后统一使用行式网格；展开英文候选的释义固定显示在网格下方，只随高亮内容更新，不再因原竖排偏好出现在右侧。
- 英文释义由 Smart English 候选明确标记；中文纠音或拼音注释不再让其他中文候选误显示“暂无释义”。
- 位于最后一页时仍可展开查看前面的候选；新一轮输入继续从紧凑候选开始。

## 0.1.14 — 2026-09-04

### 候选与更新

- 将候选翻页展开修复推进到新的 Core 版本，确保仍运行 0.1.11 早期构建的安装能通过正式版或预览版频道明确发现更新；在“可展开”模式下，只有 `[` / `]` 或 `-` / `=` 真正切换候选页后才展开，新一轮输入仍从紧凑候选开始。
- 已安装用户只需在“数据与更新”下载 Core 更新；本次语言数据未变化，无需重新下载完整安装包或词包。

## 0.1.13 — 2026-09-04

### 核心更新

- 核心更新可直接在“数据与更新”中下载；设置会显示进度，按 Catalog 校验文件大小和 SHA-256，并只在校验通过后于访达中定位安装包，不再要求用户自行寻找发布页中的安装包或手工核对哈希。
- 修正已安装核心与正在运行的核心版本号、构建号相同但内部修订版本不同时的提示，明确显示待应用的是不同内部版本。
- 修复预览频道中的 Core 与已安装版本号、构建号相同但内部修订不同时仍显示“最新”的问题；Catalog 修订不同会继续提供经过校验的更新。

## 0.1.11 — 2026-09-03

### 输入与词义

- 修复“可展开”候选仍只滚动翻页的问题：候选先以紧凑形态出现，使用 `[` / `]` 或 `-` / `=` 真正翻到其他候选页后自动展开最多三页；新一轮输入重新从紧凑形态开始。
- 修复翻页键位于第一页或最后一页时覆盖正在输入内容的问题；例如 Smart English 输入 `built-in` 不再在 `-` 处丢失 `built`。
- 修复明确输入被更长补全抢占的问题；完整英文词或全大写缩写优先保留原文，例如 `WAF` 不再被 `WAFA` 排在前面。
- 中文连续输入时可按住 Shift 输入全大写缩写；缩写左右两侧的拼音继续正常匹配和组句，不必先切换到 Smart English。
- 恢复中文标点默认值，中文模式输出 `，。；：【】`，显式选择英文标点后才输出对应半角符号。
- 展开候选中的英文释义改为固定详情区：横排位于候选下方，竖排位于右侧；切换候选时窗口尺寸保持稳定，长释义按可见行截断，无释义候选显示清晰占位。
- Smart English 始终提供纠错和模糊匹配，移除行为不一致的纠错开关；扩大长单词邻键误拼和相邻字母颠倒的检索范围，保留原始输入候选。
- 中文模式下，同一段拼音的中文候选优先于非精确英文纠错，例如 `teh` 不再被 `the` 抢占；明确匹配的英文单词仍保留原有优先规则。
- 补全 agent、client、process、token、transformer 等词族的常见基础释义，避免技术含义挤掉“代理人”“过程”“代币”“变压器”等本义；批量修正普通词、缩写和词形释义，例如 coast 恢复“海岸”、stats 补回“统计数据”，watched 不再显示“手表”，最高级使用对应中文表达，同时保留有效专业用法。
- 中文自动造词直接使用用户实际选择的读音，避免重复多音字和交错输入造成读音覆盖，也避免整词选择被重复学习。
- Smart English 的拼音反查无需前缀并跟随当前全拼或双拼方案，但不继承中文反查触发键；英文模式下的 `;` 始终直接交给当前应用。中文模式仍可使用当前方案进行带前缀反查。

### 同步与备份

- 修复设置中的“立即同步”和 iCloud 同步开关未送达正在运行的 Host 的问题；请求现在复用经过认证的 Settings IPC，并在 Host 接受后才显示成功。
- 修复自定义词和 Text Expander 的本地数据库被误判为学习词库、导致“立即同步”被 Host 拒绝的问题；iCloud 增量同步只处理实际加载的 Rime 学习词库。
- 同步目录临时不可用时保留同步配置，并在后续每小时检查时重试，不再误判为用户关闭同步。
- 打开设置和启用 iCloud 同步时，在后台准备云端目录，避免文件服务响应较慢时阻塞设置窗口。
- 学习词同步不再清空输入会话或等待整轮维护；云文件读写移到后台，本机增量合并让出输入线程，保留当前拼音、候选和可撤销的学习记录。内容未变化时不重写云端快照。
- 同步取消、词库重开或另一设备的快照变更后，继续合并仍保留原有词频与学习结果；修正多轮同步逐渐拖慢候选查询的问题，过大快照不会覆盖上一次可读取的副本。
- 本地恢复记录使用写时复制保留原始学习数据库；手动 iCloud 恢复备份首次建立基线，之后只上传差分，未变化时不重复上传。备份损坏需单独确认修复，不会自动覆盖旧记录。
- 修复 iCloud Drive 在另一台 Mac 落盘时规范化文件权限、导致内容完整的恢复备份被误判为损坏的问题；新建基线使用跨设备一致的归档权限。
- 修复 Rime/LevelDB 留有标准 `lost` 恢复隔离目录时，自定义词、停用英文词和文本扩展无法应用的问题；增量备份只保留当前学习数据库状态，不复制已淘汰的恢复碎片，也不删除原目录。
- 自定义词、停用英文词和文本扩展现在直接写入本地个人数据；普通“应用更改”只重建实际变化的个人词典，未变化数据库通过 APFS 增量克隆复用，再由正在运行的 Host 原子加载，不再重复编译全部输入方案。切换方案时以设置中选择的方案为准，不再依赖旧的 Rime 选择记录。

### 安装与更新

- 数据与更新页可在正式版和预览版频道之间明确切换；预览版用于在正式发布前验证同一批 Core 和语言数据，默认仍为正式版且不会自动回退。
- 修复首次安装已提交输入源启用请求后又强制选择输入源，导致安装器以 `OSStatus -50` 失败并留下不完整安装的问题；选择始终交给用户和 macOS。
- 修复在已经安装并授权 Linnet 的开发机上进行本地构建、代码分析或完整修复后，系统输入菜单丢失 Linnet、并再次弹出启用授权的问题；临时 Host 与冻结候选不再注册为生产输入源，Complete 也不再向 PackageKit 或 Launch Services 暴露第二个 `Linnet.app` 身份。已有 App 的授权和选择保持不变，构建过程不再注销系统登记做事后清理。
- 首次安装仍只在创建唯一的 `~/Library/Input Methods/Linnet.app` 后提交一次标准启用请求；已有 App 的 Complete 修复和日常 Core 更新都不会注册、启用或选择输入源。若系统登记本身已经缺失或停用，由 macOS 键盘设置恢复，不用反复重装或注销。
- 修正社区自签版本在维护者 Keychain 锁定或普通用户未信任开发证书时被误报为 App 损坏的问题；安装器现在核对发布时冻结的身份与完整目标内容，不读取维护者 Keychain，也不要求用户把开发证书加入系统信任。
- 应用 Core 更新时，检查状态、重新检查更新不再打断正在进行的切换；只有新 Core 报告运行就绪后才显示成功。
- 补全简体中文环境下 Core 更新确认页的说明文本，不再混入英文正文。
- 修正仅更新 Core、词包未变时的更新目录冲突，并保持旧版运行时可读取更新记录。
- 过期词包恢复可清理性，保留当前、回滚和待应用数据，避免只读文件长期占用空间。
- Core 更新会先验证既有 App 身份、完整性、版本、输入源登记与语言数据，再原子替换为包内封装的完整候选；正式版和较低 build 的预览版都能沿同一在线更新路径升级。词包继续按已安装内容使用差分，不变词包直接复用；校验失败时保留现有安装，完整修复需要明确确认。安装期间不关闭 Settings 或其他应用。
- 首次从旧版全量安装切换到差分更新时保留现有 App 和词库，避免系统安装器按旧安装记录误删文件。
- 更新保留系统引用的输入法应用目录，不再将已注册的 App 入口移到暂存目录后删除。
- 修正设置窗口保持打开时，升级后误报“版本不可用”“安装需要修复”并阻塞已安装更新入口的问题。
- 完整安装包用于修复已有安装时不再强制要求注销；首次注册仍可能需要注销一次。
- Core 升级后阻止旧 Settings 窗口使用旧格式修改学习数据，避免遗漏尚未完成合并的记录；重新打开 Linnet 设置即可继续，不影响其他应用输入。

## 0.1.10 — 2026-08-30

### 候选窗与主题

- 界面主题随 Core 更新，设置预览与实际候选窗使用相同主题，不再需要更新词包；保留用户已选主题、字体和字号。
- 主题卡片展示真实候选样式，强化宣纸、月华、青岩的色彩与选中样式区别。
- 修正候选文字与英文释义重叠、选中区域空白的布局问题。

### 中文输入与英文预测

- 候选菜单打开时恢复使用 `[` / `]` 和 `-` / `=` 前后翻页；没有组合或候选菜单时，`/ , . ; ' [ ] - =` 直接交给当前应用，方案自身的拼写键保持原有含义。
- Smart English 的下一词预测候选显示与数字键选择一致的 `1–9` 序号。
- 万象继续作为中文词库和排序核心；锁定的 rime-ice 扩展表补充万象缺失的三字及以上词条，并排除重复、歧义和无法验证读音的行，改善“希尔瓦娜斯”等长专名输入。

### 设置与更新

- 设置整合为外观、输入、词典、数据与更新四页；中文与 Smart English 选项集中在输入页。
- 核心更新卡片始终保留已安装更新的应用入口，并分别显示已安装与正在运行的版本；无需退出 TextEdit、Teams、浏览器等用户应用。
- 修正本地语言数据领先于更新频道时仍提示升级的问题，并在下载前拒绝降级和相同序号的冲突数据。
- Core 更新保留已安装词包；App 或输入源注册缺失时明确提示使用完整安装包修复，不再由 Core 尝试首次注册。

## 0.1.9 — 2026-08-28

### 候选窗与英文释义

- 重新设计 Smart English 释义布局：横排释义限制在紧凑候选框内，竖排使用独立窄侧栏；长释义自动换行，名词、动词、形容词及姓名释义分行显示，减少屏幕占用。

### 设置与运行状态

- 语言包和运行时状态改为异步加载，减少打开 Settings 时的等待；版本页可分别查看已安装与正在运行 Core 的版本、build 和 revision。
- 修复 Core 文件更新后，新打开的 Settings 无法连接到仍在运行的 Linnet Host 的问题。
- 从输入法菜单再次打开已运行的 Settings 时，窗口会主动切到前台，不再停留在其他应用窗口后方。
- 从 0.1.8 过渡时，若运行中的 Core 无法证明旧应用连接已经安全释放，Settings 会明确显示本次需要一次正常登录或重启，并保持即时应用入口关闭，避免再次让 TextEdit 等应用失去键盘输入；运行过新 Core 后，后续更新仍可直接应用。

### 更新与稳定性

- 安装 Core 更新时不再停止当前 Host、重新注册输入源或断开现有应用的输入连接；满足安全条件时可由用户主动应用已安装更新，否则当前输入继续工作，新 Core 留待下次登录或重启生效。
- Host 继续复用进程级中文词典与英文数据预热，减少新应用第一次显示候选时的等待，并避免构建或缓存副本被误启动为正式输入 Host。

### 从 0.1.8 升级

- 安装本身不要求注销，也不要求退出正在使用的应用。公开 0.1.8 Host 尚不支持新的安全应用协议；若 Settings 无法立即应用新 Core，本次新代码会在下一次登录或重启后生效。运行过 0.1.9 后，后续更新才能使用新的无注销安全应用流程。

## 0.1.8 — 2026-08-27

### 输入体验

- 修复新应用第一次输入中文时候选框明显延迟的问题；词典和模型资源现在由 Linnet Host 进程预热并保持复用，每个应用仍保留独立的未上屏文本、光标和候选会话。
- 中文模式支持连续输入“学习 CS 急停”“了解 AI 技术”“使用 CPU 性能”等中英文混合短语；普通中文拼音仍优先，单独输入缩写时继续保留中英文候选。
- 自然码下单独输入 `a` 时将常用字“啊”排在首位，并保留用户选择其他单字后的词频学习。
- 修复在 Settings 选择全拼后状态栏仍显示双拼、实际输入仍沿用双拼的问题；八种全拼和双拼方案现在都按已选设置切换。
- 中文或 Smart English 中仍有未上位字母时轻按 Shift，会原样提交已输入字母再切换模式，不再自动选择中文首候选或接受英文补全。
- 修复候选窗切换布局后高度残留、底部出现空白，以及鼠标命中区域与实际绘制范围不一致的问题。
- 候选悬停、按下、滚轮翻页和分页按钮现在共享同一交互状态；点击候选窗外仍交给当前应用处理。

### 输入源与候选稳定性

- 恢复单一 Linnet 系统输入源；全拼、双拼和 Smart English 继续由 Linnet 内部管理，不再注册为相互竞争的系统级输入源。
- 修复切换到其他输入法、Caps Lock 切换或快速返回 Linnet 后偶发只能输出 ASCII、只能输入一次或候选框消失的问题；输入源激活和每个输入框的候选发布均重新由 macOS InputMethodKit 生命周期管理。
- 修复应用输入方案、会话回收和延迟回调交错时旧状态覆盖当前候选的问题；过期会话不再干扰当前输入框。

### 设置稳定性

- 修复在 Settings 的自定义词、停用词和 Text Expander 中编辑或删除行时偶发闪退、删除错行的问题；切页和连续增删也不会访问已删除的行。
- 候选外观预览遇到瞬时布局不完整时不再终止 Settings。
- 修复存在未应用更改时选择“丢弃更改”后窗口仍无法关闭的问题；“继续编辑”、应用和丢弃现在分别执行对应动作。
- 输入页原本只有一个选项的“高级”折叠区改为直接显示，辅助码单字优先开关无需额外展开即可操作。

### 安装与更新

- Core 覆盖更新不再终止、后台启动或隐藏正在服务各应用的 Linnet Host；现有输入连接、候选框和模式提示保持可用，新版本在 Host 下次自然启动时生效。
- 仅在首次安装尚无 Linnet 记录时注册固定 App 地址；后续更新不再重复注册、启用或选择输入源，无需再次注销或重新添加，也不会重复弹出输入源授权。
- Host、Settings 与内嵌运行库使用长期固定的免费 CMS 身份；安装和更新不会要求用户输入 Keychain 密码。

## 0.1.7 — 2026-08-23

### 输入体验

- 减少空闲输入状态下不必要的光标位置查询和空文本提交，降低候选关闭、模式切换及跨应用输入时的延迟与超时风险。

### 个人数据

- iCloud Drive 学习词同步改用产品固定的 `iCloud Drive/Linnet` 目录；用户启用后无需再选择文件夹，并继续按每小时最多一次进行 Rime 增量同步。

## 0.1.6 — 2026-08-22

### 输入体验

- 修复没有候选词时按 Shift 切换中英文不显示光标旁状态提示的问题。

### 个人数据

- Settings 新增可选的 iCloud Drive 学习词同步：直接复用 Rime 的多设备增量合并，自动检查严格限制为每小时最多一次，也可由用户立即触发。
- 自动同步只处理 Rime 学习词，不会自动上传完整备份、设置、自定义词、停用词或 Text Expander；完整个人数据仍通过独立的手动恢复归档传输。

## 0.1.5 — 2026-08-21

### 英文输入

- Settings 的英文页新增“按空格键上屏候选时附加空格”开关；默认开启以保持既有连续输入体验，关闭后 Space 只提交当前候选，不再自动补尾随空格。

## 0.1.3 — 2026-08-21

### 更新体验

- Settings 打开时会读取同仓库的固定 Catalog，显示 Core、语言数据或“已是最新”状态，并支持手动重新检查。
- Core 提醒只打开经过 Catalog 绑定的同仓库更新页，不会后台静默安装未签名软件；已经完成首次安装和一次注销的用户，后续 Core 更新无需再次注销。
- 语言数据更新频道已启用；只有用户明确点击后才下载语言包，并继续使用校验、事务、原子激活和失败回滚。

## 0.1.2 — 2026-08-21

### 安装体验

- 首次安装整合为一个完整安装包，用户不再需要辨认或手动组合 Core 与语言包。

## 0.1.1 — 2026-08-20

Linnet 的首个公开社区版本。

### 双语输入体验

- 以一个 macOS 输入源承载中文、Smart English 与原始 ASCII 三种状态。
- 轻按左或右 Shift 在当前中文方案与 Smart English 之间切换；Caps Lock 保留原始 ASCII。
- 提供紧凑的模式提示、稳定的候选导航，以及横排或竖排候选布局。

### 中文与拼音反查

- 提供全拼、自然码、小鹤、微软、搜狗、智能 ABC、紫光和拼音加加八种方案，共享中文词典与学习数据。
- 中文候选使用锁定的 Wanxiang 数据，并叠加可审计的 Linnet 读音与排序修正。
- 支持用当前全拼或双拼编码反查英文；触发键可选择 `;` 或 `|`。
- 支持简繁、Emoji、中英文标点、辅助码、符号命令、Unicode 输入和本地计算器。

### Smart English

- 提供前缀补全、拼写建议、IPA、中文释义、上下文排序与下一词预测。
- 保留大小写、连续英文空格，以及 URL、邮箱、路径、版本号和代码标识符的原始形式。
- 原始输入始终保留为安全候选；Tab 可设为智能接受、候选导航或交给当前应用。
- 支持自定义词、禁用词、Text Expander，以及中英文学习数据的独立管理。

### 外观、设置与个人数据

- 提供宣纸、月华、青岩、陶印、雾青、原生玻璃和墨朱七套浅色/深色主题。
- 内置原生 Settings，可管理输入、词典、英文、外观、数据与本地候选预览。
- 支持个人数据导入导出、自动备份、恢复及中英文学习数据清理。
- 输入进程保持离线；语言数据更新只允许由用户在 Settings 中主动发起。

### 系统支持

- 支持 Apple Silicon Mac 与 macOS 13 及以上版本。
